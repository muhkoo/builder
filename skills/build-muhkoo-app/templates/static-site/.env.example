# Copy to .env.local and fill in (only needed if the site uses the email-list form).

# The deployed `subscribe` function URL. Leave unset to hide the signup form.
#   https://subscribe--<slug>.fns.muhkoo.dev
VITE_SUBSCRIBE_URL=
